package org.revature.revbook;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Main Class Spring Boot Application for "RevBook":
@SpringBootApplication
public class RevBookApplication {
    public static void main(String[] args) {

        // Call the Spring Boot Application to start and execute all processes:
        SpringApplication.run(RevBookApplication.class, args);

    }
}
